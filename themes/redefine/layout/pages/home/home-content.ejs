<div class="home-content-container">
    <%
    /* 文章排序完全交给 Hexo 原生的 front-matter `sticky`：
         sticky 3 -> 公告板（永远第一）
         sticky 2 -> 技术类文章
         sticky 1 -> 其余置顶文章
         未设置   -> 按时间倒序
       改顺序只需改各篇文章的 sticky 值，不要再在模板里排序。 */
    %>
    <ul class="home-article-list">
        <% page.posts.forEach(post => { %>
            <li class="home-article-item">

                <%
                /* 右侧竖排大字水印（英文）
                   取值优先级：
                     1. 文章 front-matter 的 watermark（可按篇指定）
                     2. theme.card_watermark[分类末段]（「信息学/算法」→「算法」）
                     3. theme.card_watermark[标签末段]
                     4. theme.card_watermark.default
                   英文短语用空格分隔，空格会渲染成竖排的间隔 */
                var wmKey = '';
                if (post.categories && post.categories.length) {
                    wmKey = post.categories.toArray()[0].name || '';
                } else if (post.tags && post.tags.length) {
                    wmKey = post.tags.toArray()[0].name || '';
                }
                if (wmKey.indexOf('/') > -1) {
                    var wmP = wmKey.split('/');
                    wmKey = wmP[wmP.length - 1];
                }
                var wmMap  = theme.card_watermark || {};
                var wmText = String(post.watermark || wmMap[wmKey] || wmMap['default'] || 'ESSAY').toUpperCase();
                var wmChars = wmText ? wmText.split('') : [];
                %>
                <% if (wmChars.length) { %>
                <div class="home-article-watermark" aria-hidden="true">
                    <% wmChars.forEach(function (ch) { %><span><%= ch %></span><% }) %>
                </div>
                <% } %>

                <% if (post.sticky) { %>
                    <div class="absolute top-[12px] right-[12px] text-[0.7rem] text-third-text-color rounded-medium bg-background-color-transparent-15 backdrop-blur-sm py-[2px] px-[10px] border border-border-color z-3 hover:text-first-text-color cursor-default">
                        <i class="fa-regular fa-thumbtack mr-[3px] text-[0.65rem]"></i><%- __('sticky') %>
                    </div>
                <% } %>

                <%  let articleThumbnail = "";
                if (post.cover || post.banner || (post.thumbnail && post.thumbnail !== false)) { %>
                    <%
                    if (post.thumbnail && post.thumbnail !== null) {
                      articleThumbnail = post.thumbnail;
                    } else if (post.cover && post.cover.includes("/")) {
                      articleThumbnail = post.cover;
                    } else if (post.banner && post.banner.includes("/")) {
                      articleThumbnail = post.banner;
                    } else if (config.marked.postAsset && config.marked.postAsset == true) {
                      articleThumbnail = [post.path, post.cover || post.banner].join("/");
                    }
                    %>
                <% } %>

                <% if (post.thumbnail !== false && (post.thumbnail || post.cover || post.banner)) { %>
                    <div id="home-article-thumbnail" class="home-article-thumbnail relative h-[150px] overflow-hidden rounded-t-large">
                        <a href="<%- url_for(post.path) %>">
                            <img src="<%- url_for(articleThumbnail) %>" alt="<%= post.title %>" class="w-full h-full object-cover dark:brightness-75 transition-all" />
                        </a>
                    </div>
                <% } %>
                <div class="flex flex-col gap-5 px-7 pb-7 <%= post.thumbnail !== false && (post.thumbnail || post.cover || post.banner) ? "pt-5" : "pt-7" %>">
                    <h3 class="home-article-title">
                        <a href="<%- url_for(post.path) %>">
                            <%= post.title %>
                        </a>
                    </h3>

                    <div class="home-article-content markdown-body">
                        <% 
                        // 检测文章是否以 blockquote 开头（前引）
                        var contentFirstLines = post.content ? post.content.trim() : '';
                        var epigraphMatch = contentFirstLines.match(/^<blockquote>\s*<p>\s*(.+?)<\/p>/);
                        if (epigraphMatch) { 
                        %>
                          <blockquote class="allposts-epigraph"><%= epigraphMatch[1] %></blockquote>
                        <% } else if (post.excerpt && post.excerpt == "false") { %>
                        <% } else if (post.excerpt && post.excerpt !== "false") { %>
                            <%- render(post.excerpt, "markdown") %>
                        <% } else { %>
                            <%
                            /* 卡片摘要：只露"一句话"。
                               优先截到第一个句末标点；否则硬截到 excerpt_length 个字符。
                               目的是让卡片保持留白与文艺感，不堆大段正文。 */
                            const excerptLength = theme.home.excerpt_length || 60;
                            let plainText = strip_html(post.content || "").replace(/\s+/g, " ").trim();
                            // 正文常以 H1 重述标题，先去重，否则摘要会等于标题
                            const ttl = String(post.title || "").replace(/\s+/g, " ").trim();
                            if (ttl && plainText.indexOf(ttl) === 0) {
                                plainText = plainText.slice(ttl.length).trim();
                            }
                            const stop = plainText.search(/[。！？!?]/);
                            if (stop > 0 && stop + 1 <= excerptLength) {
                                plainText = plainText.slice(0, stop + 1);
                            } else if (plainText.length > excerptLength) {
                                plainText = plainText.slice(0, excerptLength) + "…";
                            }
                            %>
                            <%- plainText %>
                        <% } %>
                    </div>

                    <%- partial('pages/home/home-article', {post: post}) %>
                </div>
            </li>
        <% }) %>
    </ul>

    <div class="home-paginator px-7 py-5">
        <%- partial('utils/paginator', {pageObject: page}) %>
    </div>
</div>
